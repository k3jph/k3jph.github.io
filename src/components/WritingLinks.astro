---
import { sitePath } from '../lib/site';

const { current } = Astro.props as { current?: 'writing' | 'subjects' | 'series' | 'blog' };
const links = [
  { key: 'writing', label: 'Writing', route: '/writing/' },
  { key: 'subjects', label: 'Subjects', route: '/writing/subjects/' },
  { key: 'series', label: 'Series', route: '/writing/series/' },
  { key: 'blog', label: 'Complete Blog Archive', route: '/blog/' },
];
---

<nav class="writing-links" aria-label="Writing navigation">
  {links.map((link) => current === link.key
    ? <span aria-current="page">{link.label}</span>
    : <a href={sitePath(link.route)}>{link.label}</a>)}
</nav>

<style>
  .writing-links { display: flex; flex-wrap: wrap; justify-content: center; gap: var(--space-2) var(--space-4); padding: var(--space-5) 0; font: 700 .9rem var(--font-sans); }
  .writing-links span { color: var(--color-slate-700); }
</style>
